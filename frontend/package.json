{
  "name": "dpmb-trips-frontend",
  "version": "1.0.0",
  "description": "Frontend for DPMB tram trip tracking",
  "private": true,
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "axios": "^1.4.0"
  },
  "scripts": {
    "build": "echo 'Frontend is built with Vite or similar'",
    "start": "echo 'Use npm start in root to run both backend and frontend'"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
